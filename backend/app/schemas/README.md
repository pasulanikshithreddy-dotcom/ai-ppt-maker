# Schemas

Keep Pydantic request and response models here, separated from internal domain
models in `app/models/`. Database row schemas for Supabase-backed reads also
live here when they are shared across repositories and services.
