# Services

Use this folder for AI workflows, export jobs, third-party integrations, and
domain logic. Integration clients are intentionally created lazily to keep
startup lightweight, with direct query logic delegated to repositories when it
makes the code easier to keep modular.
